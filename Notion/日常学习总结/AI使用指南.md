---
notion-id: 3b48cdcb-81ea-8086-b12e-e319cf356123
tags:
  - 工具链
  - AI
  - Claude_code
  - skill
  - MCP
  - subagent
  - autoMemory
  - compact
  - Superpowers
---

# AI使用指南

> 相关笔记：[[工具链与杂项]]

> **📑 本文件跳转目录**
> - **Claude Code**：[[AI使用指南#1. Claude Code|Claude Code]]
> - **Skill**：[[AI使用指南#2. Skill|Skill]]
> - **下载的好用 Skill**：[[AI使用指南#2. Skill#Superpowers|Superpowers]]
> - **小技巧**：[[AI使用指南#3. 使用小技巧|使用小技巧]]

## 1. Claude Code

### 工作模式
1. **默认模式（Manual Mode）**：修改文件前一定询问用户。
2. **自动模式（Accept Edits Mode）**：自动修改文件，但在终端中执行命令时会停下等待许可。
3. **全自动模式（Auto Mode）**：完全解锁模式，不再询问。
4. **规划模式（Plan Mode）**：只讨论，不修改文件。

### 常用命令
- 在框中执行 bash 命令：命令前面加上 `!`。
- `claude -c`：打开 Claude 并恢复上一次的对话。
- `/compact`：将上下文内容压缩，并转到新的页面。
- `/clear`：清除所有上下文内容。
- `/resume`：查看所有对话。
- `/init`：生成 CLAUDE.md。无论是否压缩上下文，Claude Code 都跟某个对话绑定，需用 `/init` 生成配置。
- `/hooks`：给 AI 设定"自动触发器"和"安全防火墙"，允许 AI 执行完特定操作后自动运行自定义的本地脚本或命令。

### 核心概念
- **MCP**：给 AI 插件化地装上双手和眼睛，让 AI 能安全、标准化地连接各种外部数据源和工具。
- **subagent**：独立的 agent，具有独立的上下文和 skill，独立完成某一件事。启动时会开辟全新的对话窗口，所有对话和分析过程都不会回传到主对话页面。

## 2. Skill

### Skill 是什么
- **Skill**：相当于 Claude Code 的"技能包"，本质上是一个 markdown 文件，里面写了一套针对特定任务的提示词和工作流程。
- **Skill vs Plugin**：skill 本质是一种能力模块；plugin 本质上是打包能力模块和其他配置的容器。
- **生存范围**：
  1. **全局常驻型 skill**：覆盖范围是整个对话。
  2. **阶段/任务型 skill**：直到相关任务结束才会停止。

### Superpowers
开发阶段四步：
1. **需求与架构**：明确意图，评估方案利弊（无法输出任何代码）。
2. **拆解与隔离**：创建独立开发环境，将大需求拆为细粒度卡片（没有任务 Plan，绝不下笔）。
3. **驱动编码**：逐个任务卡片写测试、写实现、重构（没看到测试 FAIL，绝不写功能代码，强制执行红绿重构流程，即 TDD 原则）。
4. **排错与交付**：定位根因、修复 Bug、响应 Code Review、提 PR。

## 3. 使用小技巧

- 上下文转接：
    - compact:压缩大量当前对话 -> 把大量原始对话和过程记录替换成一份更短的摘要
         适用场景：仍需要在当前对话进行工作，但又不得不释放上下文空间
         [[转接文件库#通用带目标compact提示词|通用带目标compact提示词]]
         缺点：关键信息会在压缩过程中消失
     -  handoff:很适合一次性交接 -> 现在做到了哪里，哪些测试没做，下一步做什么
          缺点：不断转述，细节随着增加会减少
     - CLAUDE.md:放长期稳定，每个对话都要遵守
          放置：项目背景，技术栈，代码规范，目录约定
          不该放排查记录，bug查到哪一步
      -  原生autoMemory + 结构化整理规则 = 结构化记忆
         核心工作流程：初始化提示词 -> 旧对话收尾提示词 -> 新对话开始提示词 -> 定期维护提示词
          - [[转接文件库#初始化提示词|初始化提示词]]
          - [[转接文件库#旧对话收尾提示词|旧对话收尾提示词]]
          - [[转接文件库#新对话开始提示词|新对话开始提示词]]
          - [[转接文件库#定期维护提示词|定期维护提示词]]
          
	 - [[转接文件库#三层分类提示词|三层分类提示词]] -> 将保存的关键信息所在的文件进行检查与更改
	 - [[转接文件库#记忆目录迁移提示词|记忆目录迁移提示词]] -> 将autoMemory的文件位置放置在项目的根目录内
	 - [[转接文件库#对话保留时间延长提示词|对话保留时间延长提示词]] 
    